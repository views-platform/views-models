"""Scaffold builders: create a new model, ensemble, or package from templates.

The entry point for adding anything to `models/`, `ensembles/` or a sibling package.
Governed by CICs (`docs/CICs/ModelScaffoldBuilder.md` and siblings) because what they
emit becomes the shape every later tool assumes.
"""
